include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    # The example app has its own analysis_options.yaml and is analyzed as its
    # own package by CI's example job.
    - "example/**"
